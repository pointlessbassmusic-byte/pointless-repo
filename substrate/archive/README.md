# Substrate engine — version lineage archive

Historical artifacts from the original claude.ai chat, preserved for provenance.
The authoritative code is the parent directory (== handoff v1.0). Nothing here runs in
production.

| File | What it is |
|---|---|
| `substrate_engine_v0.1.zip` … `v0.3.zip` | Earlier full snapshots of the engine |
| `parallel_engines.py` | v0.1-era: Substrate (A) + Echo (B) running side by side vs. NULL/LIVE worlds |
| `wwwwwh_models.py` | v0.2-era: earlier, larger WWWWWH category models |
| `lookback_trap.py` | Methodology demo: why the engine cannot be validated on historical data (28,800-analysis garden-of-forking-paths trap vs. preregistration) |
