# External signals — sources, first correlations, discipline (2026-09-17)

New package `sportsbot/signals/` (data only — nothing is wired into trading;
strategy changes still require backtest/CLV evidence).

## Source policy (probed from this environment)

| source | verdict |
|---|---|
| NWS `api.weather.gov` | **Adopted.** Free public API intended for programmatic use; point forecasts for the 7 Kalshi settlement stations. |
| Bluesky public search (`app.bsky.feed.searchPosts`) | **Adopted.** Open, unauthenticated endpoint; descriptive User-Agent, ≤3 pages/query, 2s pauses, one polite retry on rate limit. |
| Reddit JSON endpoints | **Rejected.** Refuses datacenter clients (HTML interstitial). We respect the access control rather than evade it. |
| X/Twitter | Not attempted — no free API tier for this use. |

## Signal 1: NWS forecast → weather-arm baseline (accuracy-relevant now)

- `signals/nws.py`: forecast high per station/date, converted to P(YES) with a
  normal error model, whole-degree continuity correction, and **lead-aware
  sigma** (`sigma_for_lead`: 1.8°F ramping ~0.55/day, capped 5.5).
- The snapshot service now records forecasts each cycle **before** market rows
  (`weather_forecasts` table), and the exporter uses only forecasts recorded
  **at or before a market's first (max-lead) snapshot** — never backfilled
  (unit-tested both ways). Existing 168 markets therefore keep climatology
  (`nws_rows: 0` on the historical export — the discipline, not a bug);
  every newly sighted market gets the forecast baseline.

**First live correlation** (84 open weather markets, forecast prob vs market
mid, split by lead):

| bucket | n | Pearson | MAE |
|---|---|---|---|
| next-day+ (the substrate's decision regime) | 42 | **+0.862** | 0.069 |
| same-day (market has intraday info) | 42 | +0.126 | 0.203 |

Reading: at the max-lead decision point the forecast is a strong conventional
baseline — expect it to land **between climatology (Brier 0.193) and the
market (0.052)** once settled cohorts accrue; the martingales will measure
exactly where. Same-day markets are the wrong regime for a forecast error
model, and the split shows the pipeline knows it.

## Signal 2: social chatter (research feed; profitability TBD by evidence)

- `signals/chatter.py` + `sportsbot signals-scan/--report`: per-entity post
  counts and event-vocabulary flags (injury/scratched/walkover/rain-delay…)
  from Bluesky public search, timestamped at collection into
  `data/chatter.sqlite`.
- First live scan: "Dodgers" → 100 posts/24h, **7 injury-flagged**
  (injury×5, injured×4); "Carlos Alcaraz" → 18 posts. Signal exists; whether
  it predicts anything is an open question the harness is built to answer:
  `sportsbot signals-report --events <ingest.csv>` joins only chatter
  collected **before** each event's decision time against market residuals
  (|outcome − market_prob|), Spearman, and refuses to opine under n=30 pairs.

## How this becomes profit (or gets killed)

1. Run `signals-scan` on a cron alongside the paper loop; pairs accrue as
   events resolve after collection.
2. `signals-report` weekly. |ρ| that persists across weeks on growing n is a
   candidate feature; noise gets dropped.
3. Any strategy change still goes through the walk-forward backtest and the
   CLV gate — a story about chatter is not evidence; the report is.

## Scoring the arms

`sportsbot weather-score` prints the decision-time Brier of every arm on
settled markets — coin, climatology, NWS forecast, market — scored at each
ticker's max-lead first sighting under the same no-backfill rule, so the
comparison is like-for-like. The `NWS-covered` block is the subset where a
decision-time forecast existed, which is the only cohort where NWS vs
climatology is a fair contest; `export_ingest_csv` collapses the same inputs
into one priority-ordered `baseline_prob` for substrate ingest, and this
command is how you see the arms apart.

As of 2026-09-22 (168 settled rows, all predating forecast recording):

| cohort | n | coin | climatology | NWS | market |
|---|---|---|---|---|---|
| all settled | 168 | 0.2500 | 0.1828 | — | 0.0751 |
| 2026-09-17 | 84 | 0.2500 | 0.1928 | — | 0.0519 |
| 2026-09-21 | 84 | 0.2500 | 0.1728 | — | 0.0984 |

The triple-null hierarchy holds (coin > conventional > market). The NWS column
is empty on purpose: forecast recording began 2026-09-17 17:35Z and every
settled market was first sighted before that, so the no-backfill query refuses
them. The 84 open markets targeting Sep 21–22 do carry decision-time
forecasts — they are the first cohort that can fill that column.

### 2026-09-22: the first NWS-covered cohort, scored

The Sep-21-target markets settled, and they are the first cohort whose
decision-time snapshot had an NWS forecast on file. All four arms, scored at
the same decision point:

| arm | Brier | n |
|---|---|---|
| coin | 0.2500 | 18 |
| climatology | 0.1674 | 18 |
| **NWS forecast** | **0.1431** | 18 |
| market | **0.0121** | 18 |

Two things to read from it, and the second matters more than the first.

**The forecast arm beat climatology, but not by anything you could call a
result.** Those 18 rows are **3 independent events** — three stations on one
target date, six strike bands each — and the bands of a station-day are near
deterministic functions of the same temperature outcome. The paired
per-row difference (climatology squared error minus NWS squared error) is
+0.0243 with an sd of 0.1447: t = 0.7 treating the bands as independent,
t = 0.3 at the honest n of 3. NWS was better on 10 of 18 bands. The ordering
is in the direction the design predicts and that is all it is; one more
station-day either way would move it.

There is also a regime caveat. The lead on this cohort was **-0.79 days** —
the first sighting came after the target day had already begun, so these are
same-day forecasts, which is exactly the regime where the correlation study
above already measured the NWS arm as weak (+0.126 same-day against +0.862
at next-day-or-more lead). A fair test of the forecast arm needs cohorts
sighted a day or more ahead.

**The market beat every arm by an order of magnitude** — 0.0121 against
climatology's 0.1674 — and that part is not marginal. It is consistent with
the full 186-row picture (market 0.0690, climatology 0.1813, coin 0.2500) and
it is why the weather arm stays excluded from trading in
`sportsbot/bot/allocation.py`: whatever we know about tomorrow's high, the
price knows it better. The arm earns its keep as a substrate measurement, not
as a source of bets.

Data: `substrate/reports/weather_nws_cohort_2026-09-22.csv` (per-row NWS,
climatology, market and outcome). Reproduce with `sportsbot weather-score`.

### 2026-09-23: the full Sep-21 cohort — the forecast arm does beat climatology

All seven stations for target 2026-09-21 have now settled, so the cohort is
complete: 42 rows, 7 station-days.

| arm | Brier | n |
|---|---|---|
| coin | 0.2500 | 42 |
| climatology | 0.1929 | 42 |
| **NWS forecast** | **0.1280** | 42 |
| market | **0.0518** | 42 |

Paired properly — one observation per station-day, since the six strike bands
of a station-day are near deterministic functions of the same temperature:

```
n = 7 station-days
mean (climatology SE - NWS SE) = +0.0650   sd 0.0547   se 0.0207
t = +3.15      95% CI [+0.0245, +0.1055]
NWS better on 7 of 7 station-days
```

Directionally unanimous and, at this level of pairing, significant. **But the
seven station-days are all the same date**, and seven US stations on one day
share a continental weather pattern: if that day happened to be well
forecast, all seven benefit together. The effective independent sample is
therefore closer to *one day* than to seven, and the t-statistic above should
be read as optimistic. What this cohort establishes is direction, not
magnitude. Multiple target **dates** are what would settle it, and the
snapshot service accumulates those only when it runs continuously.

The lead was also -0.79 days — the first sighting came after the target day
began, so these are same-day forecasts, the regime the correlation study
measured as weakest (+0.126 same-day vs +0.862 at a day or more). The arm
beating climatology *in its worst regime* is the encouraging part.

None of this changes the trading position. The market scored 0.0518 against
the forecast arm's 0.1280 — still better by a factor of 2.5 — so the weather
arm stays excluded from `sportsbot/bot/allocation.py` and remains a substrate
measurement. Knowing tomorrow's high better than climatology does is not the
same as knowing it better than the price.

Data: `substrate/reports/weather_nws_cohort_2026-09-23.csv`.

## Retro chatter pilot — collection, and why it stopped

`sportsbot signals-retro` collects **pre-decision** chatter windows for
already-resolved events (Bluesky `since`/`until` bounded at each event's
decision time, so every joined post predates the decision — leak-free with
respect to outcomes; still retrospective, so hypothesis-generating only).

- Eligible: **21** of the 265 resolved season events. Only plain
  `mlb-<away>-<home>-<date>` moneyline slugs map to team names with confidence:
  219 events are `fpd` table-tennis fast leagues, 14 are tennis futures, and 7
  are spread/total derivatives of MLB games already counted (including those
  would join the same game's chatter window to a second, non-independent
  outcome). An earlier revision of this doc said 36 — that was a miscount of
  the prefix groups, not a shrinking pool.
- Collected: **17 events, 1,124 pre-decision posts, 57 flagged** across three
  passes (2026-09-21). Rows persist to `substrate/reports/retro_chatter_*.csv`
  so collections survive environment resets. n < 30 ⇒ **no correlation is
  claimed**. One illustrative (not evidentiary) case: `mlb-stl-cin-2026-05-24`
  carried 5 flagged posts pre-game against a near-coin market (0.505).

### 2026-09-22: the pilot cannot reach its own evidence bar

Two things closed this path, and both are worth stating plainly rather than
leaving the study "pending" forever:

1. **The pool is too small by construction.** 21 eligible events is the
   ceiling from the season export (17 collected, 4 still uncollected) — nine
   short of the n ≥ 30 bar the report refuses to publish below. No amount of
   collection time fixes that; the bar is unreachable until the bot settles
   more MLB *moneyline* events, which accrue slowly from live paper trading.
2. **Bluesky now refuses this environment.** Every bounded-window search
   returns `403 Forbidden` from both `public.api.bsky.app` and `api.bsky.app`
   (2026-09-22), where the same queries returned data on 2026-09-21. That is
   an access control, not a rate limit, and per the source policy above we do
   not work around it — no auth borrowing, no proxying, no scraping the web UI.

Status: the chatter arm is **shelved as inconclusive**, not disproven. The
collected 17 rows stay in the repo as a record. It becomes testable again only
if both unblock — a larger settled MLB moneyline pool AND a permitted access
path (an authenticated Bluesky app password the account owner supplies, or a
host the endpoint serves). Until then the NWS weather arm, which has a real
measured result and a settling cohort, is the signals layer's evidence channel.

`retro_report` still prints the Spearman + flagged-vs-quiet residual
comparison the moment n ≥ 30 is reached, and it refuses below that on its own
— nothing here weakens that gate. The live accrual path (`signals-scan` +
`signals-report`) remains the primary evidence channel either way.
